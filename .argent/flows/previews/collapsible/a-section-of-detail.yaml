executionPrerequisite: >-
  The chrome-free preview route is open on collapsible/a-section-of-detail and
  has settled — one closed row reading What is included. The capture script
  navigates there before running this.
steps:
  - echo: Opening the section
  - tap: { id: included }
  - wait: 1000
  - echo: And closing it again, so the clip loops without a jump cut
  - tap: { id: included }
