---
title: BottomSheet
description: A sheet that rises from the bottom edge, built on @gorhom/bottom-sheet.
icon: PanelBottom
---

## Installation

<ComponentInstall name="bottom-sheet" />

## Usage

```tsx
import { BottomSheet } from "delacour-react-native-ui/bottom-sheet";
```

```tsx
<BottomSheet>
  <BottomSheet.Trigger asChild>
    <Button>Open</Button>
  </BottomSheet.Trigger>

  <BottomSheet.Portal>
    <BottomSheet.Overlay />
    <BottomSheet.Container>
      <BottomSheet.Content>
        <BottomSheet.Title>Filters</BottomSheet.Title>
        <BottomSheet.Description>Narrow the list down.</BottomSheet.Description>
        <BottomSheet.Close />
      </BottomSheet.Content>
    </BottomSheet.Container>
  </BottomSheet.Portal>
</BottomSheet>
```

## Controlled

`onOpenChange` fires whenever the sheet opens or closes — a trigger, a swipe down, a press on the
backdrop, `BottomSheet.Close`, or a controlled `isOpen`.

```tsx
<BottomSheet isOpen={isOpen} onOpenChange={setIsOpen}>…</BottomSheet>
```

## Scrolling

`BottomSheet.ScrollView` needs `enableDynamicSizing={false}` and explicit `snapPoints` on the
container, or the sheet grows to its content and there is nothing to scroll within.

```tsx
<BottomSheet.Container enableDynamicSizing={false} snapPoints={["50%", "90%"]}>
  <BottomSheet.ScrollView>{rows}</BottomSheet.ScrollView>
</BottomSheet.Container>
```

## Footer

A footer is in the flow by default; `sticky` pins it to the sheet's bottom edge and above the
keyboard.

```tsx
<BottomSheet.Footer sticky>
  <Button onPress={apply}>Apply</Button>
</BottomSheet.Footer>
```

<Callout type="warn">
Do not put a `KeyboardStickyView` inside a sheet. It is a third mechanism moving the same view, and
it fights the other two every frame.
</Callout>

## Text input

Use `useBottomSheetInput()` and spread what it returns onto the field.

```tsx
import { useBottomSheetInput } from "delacour-react-native-ui/bottom-sheet";

const input = useBottomSheetInput();

<Input {...input} value={query} onChangeText={setQuery} />;
```

<Callout type="warn">
The `ref` it returns is not optional polish: without it, moving focus between two fields in one
sheet reads as the keyboard closing and reopening, and the sheet resizes twice on the way. A *drag*
across a field can still be claimed by the sheet's content pan — pass
`enableContentPanningGesture={false}` on the container when that bites.
</Callout>

## API Reference

### Requirements

`@gorhom/bottom-sheet` is a **required** peer, and the app must mount
[`DelacourProvider`](/docs/native/components/provider) — it supplies the modal provider the sheet
presents into.

### Anatomy

| Part | What it is |
| --- | --- |
| `BottomSheet` | State only — the root renders no view at all |
| `BottomSheet.Trigger` | The control that opens the sheet. `asChild` to make a `Button` the trigger |
| `BottomSheet.Portal` | Everything drawn above the app. Lifts the overlay out to the container |
| `BottomSheet.Overlay` | The scrim. Written beside the container, drawn as its backdrop |
| `BottomSheet.Container` | The sheet itself. Every gorhom prop passes through it |
| `BottomSheet.Content` | The sheet's body, sized to itself under `enableDynamicSizing` |
| `BottomSheet.ScrollView` | A scrolling body. Needs `enableDynamicSizing={false}` and snap points |
| `BottomSheet.Close` | The dismiss control, positioned out of the content's flow |
| `BottomSheet.Title` | The sheet's heading — a `Text.Header` with the close control's clearance |
| `BottomSheet.Description` | Supporting copy under the title |
| `BottomSheet.Footer` | Controls at the bottom. `sticky` pins them above the keyboard |

`Portal` draws its `Overlay` as the container's backdrop and `Container` draws a `sticky` `Footer`
as the sheet's footer, so those two parts render somewhere other than where they are written.

### BottomSheet

<TypeTable
  type={{
    isOpen: { type: "boolean", description: "Controlled open state. Leave it off and the sheet holds its own." },
    defaultOpen: { type: "boolean", description: "Initial open state while uncontrolled." },
    onOpenChange: {
      type: "(isOpen: boolean) => void",
      description: "Called whenever the sheet opens or closes — by a trigger, a swipe down, a press on the backdrop, BottomSheet.Close, or a controlled isOpen.",
    },
    children: { type: "ReactNode" },
  }}
/>

The root takes no `ViewProps` — it renders no view. Layout and styling belong on `Container`,
`Content` and `Footer`.

### `BottomSheet.Trigger`

Opens the sheet. A `Pressable`, so it inherits the whole vocabulary — `asChild` renders into your
own element instead of emitting a `View`.

<TypeTable
  type={{
    asChild: { type: "boolean", description: "Render into the single child element rather than wrapping it." },
    onPress: { type: "() => void", description: "Called after the sheet is told to open, not instead of it." },
    className: { type: "string" },
    children: { type: "ReactNode" },
  }}
/>

### `BottomSheet.Container`

The modal itself. Extends `@gorhom/bottom-sheet`'s `BottomSheetModalProps` — `snapPoints`,
`enableDynamicSizing`, `index` and the rest pass straight through. The component slots
(`backdropComponent`, `backgroundComponent`, `footerComponent`, `handleComponent`) and `onDismiss`
are withheld: the compound parts fill them. It takes no `className` — reach for the three below.

<TypeTable
  type={{
    backgroundClassName: { type: "string", description: "The sheet's own surface." },
    handleClassName: { type: "string", description: "The area around the grabber." },
    handleIndicatorClassName: { type: "string", description: "The grabber itself." },
    children: { type: "ReactNode" },
  }}
/>

### `BottomSheet.Content`

The padded region inside the sheet. Extends `ViewProps`.

<TypeTable type={{ className: { type: "string" }, children: { type: "ReactNode" } }} />

### `BottomSheet.ScrollView`

Scrollable content that hands its gesture to the sheet at the top of the scroll. Extends Gorhom's
`BottomSheetScrollViewProps`; `contentContainerStyle` and `enableFooterMarginAdjustment` are
withheld — the first has a `className` equivalent, the second is managed for you.

<TypeTable
  type={{
    className: { type: "string" },
    contentContainerClassName: { type: "string" },
    children: { type: "ReactNode" },
  }}
/>

### `BottomSheet.Footer`

Pinned below the content.

<TypeTable
  type={{
    sticky: { type: "boolean", description: "Keep the footer against the sheet's bottom edge as it moves." },
    className: { type: "string" },
    children: { type: "ReactNode" },
  }}
/>

### `BottomSheet.Overlay`

The backdrop. Composed in with sensible defaults when the sheet holds none.

<TypeTable
  type={{
    opacity: { type: "number", description: "The backdrop's opacity when fully shown." },
    appearsOnIndex: { type: "number" },
    disappearsOnIndex: { type: "number" },
    isCloseOnPress: { type: "boolean", description: "Whether a press on the backdrop dismisses the sheet." },
    enableTouchThrough: { type: "boolean", description: "Let touches reach what is behind the backdrop." },
    accessibilityLabel: { type: "string" },
    onPress: { type: "() => void" },
    className: { type: "string" },
  }}
/>

### `BottomSheet.Title` / `Description` / `Close` / `Handle` / `Background` / `Portal`

`Title` and `Description` are `Text` presets and take its props. `Close` is a `Pressable` that
dismisses the sheet, defaulting its `accessibilityLabel` to `"Close"` and its `feedback` to `fade`.
`Handle` and `Background` replace the grabber and the surface outright — reach for
`handleClassName` and `backgroundClassName` on `Container` first. `Portal` takes only `children`.

<TypeTable
  type={{
    className: { type: "string" },
    indicatorClassName: { type: "string", description: "`Handle` only — the grabber inside it." },
    children: { type: "ReactNode" },
  }}
/>
