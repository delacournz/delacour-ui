executionPrerequisite: >-
  The chrome-free preview route is open on spinner/sizes and has settled — seven
  spinners, already turning. The capture script navigates there before running
  this.
steps:
  - echo: Nothing to touch — a spinner is its motion, so this only holds the camera on it
  - wait: 2000
