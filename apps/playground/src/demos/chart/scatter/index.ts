import { defineDemoGroup } from "../../define-demo-group";
import * as scatter from "./scatter";

/** One demo: two series of dots on a numeric x. */
export const chartScatterDemos = defineDemoGroup("chart/scatter", {
	scatter,
});
