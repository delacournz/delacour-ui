---
"@delacour/react-native-charts": patch
"delacour": patch
---

List every native peer in the install command. The charts README's `expo install` line named only Skia, and `delacour add chart` skipped Gesture Handler — the scrub and the pie's tap then never fire, with no error.
